<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Quote Request - {{reference}}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #2c5530; color: white; padding: 20px; text-align: center; }
        .content { background: #f9f9f9; padding: 20px; }
        .field { margin-bottom: 15px; }
        .field strong { display: inline-block; width: 150px; }
        .urgent { background: #ff6b6b; color: white; padding: 5px 10px; border-radius: 3px; }
        .footer { background: #eee; padding: 15px; text-align: center; font-size: 12px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🏠 New Quote Request</h1>
            <p>Reference: <strong>{{reference}}</strong></p>
        </div>
        
        <div class="content">
            <h2>Customer Information</h2>
            <div class="field">
                <strong>Name:</strong> {{customerName}}
            </div>
            <div class="field">
                <strong>Email:</strong> <a href="mailto:{{email}}">{{email}}</a>
            </div>
            <div class="field">
                <strong>Phone:</strong> <a href="tel:{{phone}}">{{phone}}</a>
            </div>
            
            <h2>Property Details</h2>
            <div class="field">
                <strong>Property Type:</strong> {{propertyType}}
            </div>
            <div class="field">
                <strong>Estimated Area:</strong> {{estimatedArea}}
            </div>
            {{#if address}}
            <div class="field">
                <strong>Address:</strong> {{address}}
            </div>
            {{/if}}
            
            <h2>Request Details</h2>
            <div class="field">
                <strong>Urgency:</strong> 
                {{#if (eq urgency "immediate")}}
                    <span class="urgent">🚨 IMMEDIATE</span>
                {{else if (eq urgency "within-week")}}
                    <span style="background: #ffa500; color: white; padding: 3px 8px; border-radius: 3px;">⚠️ WITHIN WEEK</span>
                {{else}}
                    {{urgency}}
                {{/if}}
            </div>
            {{#if additionalInfo}}
            <div class="field">
                <strong>Additional Info:</strong><br>
                <div style="background: white; padding: 10px; border-left: 3px solid #2c5530; margin-top: 5px;">
                    {{additionalInfo}}
                </div>
            </div>
            {{/if}}
            
            <div class="field">
                <strong>Submitted:</strong> {{submittedAt}}
            </div>
        </div>
        
        <div class="footer">
            <p>This notification was sent automatically when a customer submitted a quote request on your website.</p>
            <p><strong>Action Required:</strong> Contact the customer within 24-48 hours as promised.</p>
        </div>
    </div>
</body>
</html>